{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome to MkDocs","text":"<p>For full documentation visit mkdocs.org.</p>"},{"location":"#commands","title":"Commands","text":"<ul> <li><code>mkdocs new [dir-name]</code> - Create a new project.</li> <li><code>mkdocs serve</code> - Start the live-reloading docs server.</li> <li><code>mkdocs build</code> - Build the documentation site.</li> <li><code>mkdocs -h</code> - Print help message and exit.</li> </ul>"},{"location":"#project-layout","title":"Project layout","text":"<pre><code>mkdocs.yml    # The configuration file.\ndocs/\n    index.md  # The documentation homepage.\n    ...       # Other markdown pages, images and other files.\n</code></pre>"},{"location":"autoapi/summary/","title":"Summary","text":"<ul> <li>models<ul> <li>user</li> </ul> </li> <li>tests<ul> <li>test_example</li> <li>test_user</li> </ul> </li> </ul>"},{"location":"autoapi/models/user/","title":"user","text":""},{"location":"autoapi/models/user/#models.user.User","title":"<code>User</code>","text":"<p>A simple User class to store and manage user information.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>The user's name.</p> <code>age</code> <code>int</code> <p>The user's age.</p> <code>_password_hash</code> <code>str</code> <p>The hashed password (internal use only).</p> Source code in <code>src/models/user.py</code> <pre><code>class User:\n    \"\"\"\n    A simple User class to store and manage user information.\n\n    Attributes:\n        name (str): The user's name.\n        age (int): The user's age.\n        _password_hash (str): The hashed password (internal use only).\n    \"\"\"\n\n    def __init__(self, name: str, age: int, password: str):\n        \"\"\"\n        Initializes a User instance.\n\n        Args:\n            name (str): The name of the user.\n            age (int): The age of the user.\n            password (str): The user's password in plain text.\n        \"\"\"\n        self.name = name\n        self.age = age\n        self._password_hash = self._hash_password(password)\n\n    def _hash_password(self, password: str) -&gt; str:\n        \"\"\"Hashes the password using SHA-256.\"\"\"\n        return hashlib.sha256(password.encode()).hexdigest()\n\n    def salt_password(self, salt: str) -&gt; str:\n        \"\"\"\n        Returns a salted hash of the password.\n\n        Args:\n            salt (str): A salt string to enhance password security.\n\n        Returns:\n            str: The salted and hashed password.\n        \"\"\"\n        salted = salt + self._password_hash\n        return hashlib.sha256(salted.encode()).hexdigest()\n\n    def verify_password(self, password: str) -&gt; bool:\n        \"\"\"\n        Verifies if the given password matches the stored hash.\n\n        Args:\n            password (str): The password to verify.\n\n        Returns:\n            bool: True if the password is correct, False otherwise.\n        \"\"\"\n        return self._hash_password(password) == self._password_hash\n</code></pre>"},{"location":"autoapi/models/user/#models.user.User.__init__","title":"<code>__init__(name, age, password)</code>","text":"<p>Initializes a User instance.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the user.</p> required <code>age</code> <code>int</code> <p>The age of the user.</p> required <code>password</code> <code>str</code> <p>The user's password in plain text.</p> required Source code in <code>src/models/user.py</code> <pre><code>def __init__(self, name: str, age: int, password: str):\n    \"\"\"\n    Initializes a User instance.\n\n    Args:\n        name (str): The name of the user.\n        age (int): The age of the user.\n        password (str): The user's password in plain text.\n    \"\"\"\n    self.name = name\n    self.age = age\n    self._password_hash = self._hash_password(password)\n</code></pre>"},{"location":"autoapi/models/user/#models.user.User.salt_password","title":"<code>salt_password(salt)</code>","text":"<p>Returns a salted hash of the password.</p> <p>Parameters:</p> Name Type Description Default <code>salt</code> <code>str</code> <p>A salt string to enhance password security.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The salted and hashed password.</p> Source code in <code>src/models/user.py</code> <pre><code>def salt_password(self, salt: str) -&gt; str:\n    \"\"\"\n    Returns a salted hash of the password.\n\n    Args:\n        salt (str): A salt string to enhance password security.\n\n    Returns:\n        str: The salted and hashed password.\n    \"\"\"\n    salted = salt + self._password_hash\n    return hashlib.sha256(salted.encode()).hexdigest()\n</code></pre>"},{"location":"autoapi/models/user/#models.user.User.verify_password","title":"<code>verify_password(password)</code>","text":"<p>Verifies if the given password matches the stored hash.</p> <p>Parameters:</p> Name Type Description Default <code>password</code> <code>str</code> <p>The password to verify.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the password is correct, False otherwise.</p> Source code in <code>src/models/user.py</code> <pre><code>def verify_password(self, password: str) -&gt; bool:\n    \"\"\"\n    Verifies if the given password matches the stored hash.\n\n    Args:\n        password (str): The password to verify.\n\n    Returns:\n        bool: True if the password is correct, False otherwise.\n    \"\"\"\n    return self._hash_password(password) == self._password_hash\n</code></pre>"},{"location":"autoapi/tests/test_example/","title":"test_example","text":"<p>Unit tests for basic string comparison, object equality using a custom class (mockObj), and file existence check.</p> <p>This test suite includes:</p> <ul> <li> <p>A string equality test.</p> </li> <li> <p>A test for equality of two mockObj instances.</p> </li> <li> <p>A check for the existence of a specific file on disk.</p> </li> </ul>"},{"location":"autoapi/tests/test_example/#tests.test_example.ExampleTestCases","title":"<code>ExampleTestCases</code>","text":"<p>               Bases: <code>TestCase</code></p> <p>A set of unit tests to validate string comparison, object equality, and file existence.</p> Source code in <code>src/tests/test_example.py</code> <pre><code>class ExampleTestCases(unittest.TestCase):\n    \"\"\"\n    A set of unit tests to validate string comparison, object equality, and file existence.\n    \"\"\"\n\n    def test_str(self):\n        \"\"\"\n        Tests whether two identical strings are equal.\n        \"\"\"\n        self.assertEqual(\"string is the same\", \"string is the same\", \"strs are not equal\")\n\n    def test_object(self):\n        \"\"\"\n        Tests whether two mockObj instances with the same attributes are considered equal.\n        \"\"\"\n        obj1 = mockObj(\"Chris\", 20)\n        obj2 = mockObj(\"Chris\", 20)\n        self.assertEqual(obj1, obj2, \"Given Objects are not equal\")\n</code></pre>"},{"location":"autoapi/tests/test_example/#tests.test_example.ExampleTestCases.test_object","title":"<code>test_object()</code>","text":"<p>Tests whether two mockObj instances with the same attributes are considered equal.</p> Source code in <code>src/tests/test_example.py</code> <pre><code>def test_object(self):\n    \"\"\"\n    Tests whether two mockObj instances with the same attributes are considered equal.\n    \"\"\"\n    obj1 = mockObj(\"Chris\", 20)\n    obj2 = mockObj(\"Chris\", 20)\n    self.assertEqual(obj1, obj2, \"Given Objects are not equal\")\n</code></pre>"},{"location":"autoapi/tests/test_example/#tests.test_example.ExampleTestCases.test_str","title":"<code>test_str()</code>","text":"<p>Tests whether two identical strings are equal.</p> Source code in <code>src/tests/test_example.py</code> <pre><code>def test_str(self):\n    \"\"\"\n    Tests whether two identical strings are equal.\n    \"\"\"\n    self.assertEqual(\"string is the same\", \"string is the same\", \"strs are not equal\")\n</code></pre>"},{"location":"autoapi/tests/test_example/#tests.test_example.mockObj","title":"<code>mockObj</code>","text":"<p>A simple mock object class to demonstrate equality comparison.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>The name of the object.</p> <code>age</code> <code>int</code> <p>The age associated with the object.</p> Source code in <code>src/tests/test_example.py</code> <pre><code>class mockObj():\n    \"\"\"\n    A simple mock object class to demonstrate equality comparison.\n\n    Attributes:\n        name (str): The name of the object.\n        age (int): The age associated with the object.\n    \"\"\"\n\n    def __init__(self, name: str, age: int):\n        \"\"\"\n        Initializes a mockObj instance with a name and age.\n\n        Args:\n            name (str): The name of the object.\n            age (int): The age of the object.\n        \"\"\"\n        self.name = name\n        self.age = age\n\n    def __eq__(self, other):\n        \"\"\"\n        Checks equality between two mockObj instances.\n\n        Examples:\n            &gt;&gt;&gt; mockObj1 == mockObj2\n            true\n            &gt;&gt;&gt; mockObj1 == mockObj3\n            false\n\n        Args:\n            other (mockObj): The object to compare with.\n\n        Returns:\n            bool: True if both objects have the same name and age, False otherwise.\n        \"\"\"\n        return isinstance(other, mockObj) and self.name == other.name and self.age == other.age\n</code></pre>"},{"location":"autoapi/tests/test_example/#tests.test_example.mockObj.__eq__","title":"<code>__eq__(other)</code>","text":"<p>Checks equality between two mockObj instances.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; mockObj1 == mockObj2\ntrue\n&gt;&gt;&gt; mockObj1 == mockObj3\nfalse\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>mockObj</code> <p>The object to compare with.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>True if both objects have the same name and age, False otherwise.</p> Source code in <code>src/tests/test_example.py</code> <pre><code>def __eq__(self, other):\n    \"\"\"\n    Checks equality between two mockObj instances.\n\n    Examples:\n        &gt;&gt;&gt; mockObj1 == mockObj2\n        true\n        &gt;&gt;&gt; mockObj1 == mockObj3\n        false\n\n    Args:\n        other (mockObj): The object to compare with.\n\n    Returns:\n        bool: True if both objects have the same name and age, False otherwise.\n    \"\"\"\n    return isinstance(other, mockObj) and self.name == other.name and self.age == other.age\n</code></pre>"},{"location":"autoapi/tests/test_example/#tests.test_example.mockObj.__init__","title":"<code>__init__(name, age)</code>","text":"<p>Initializes a mockObj instance with a name and age.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the object.</p> required <code>age</code> <code>int</code> <p>The age of the object.</p> required Source code in <code>src/tests/test_example.py</code> <pre><code>def __init__(self, name: str, age: int):\n    \"\"\"\n    Initializes a mockObj instance with a name and age.\n\n    Args:\n        name (str): The name of the object.\n        age (int): The age of the object.\n    \"\"\"\n    self.name = name\n    self.age = age\n</code></pre>"},{"location":"autoapi/tests/test_user/","title":"test_user","text":""},{"location":"autoapi/tests/test_user/#tests.test_user.TestUser","title":"<code>TestUser</code>","text":"<p>               Bases: <code>TestCase</code></p> <p>Unit tests for the User class.</p> Tests <ul> <li>Attribute initialization</li> <li>Password verification</li> <li>Salted password generation</li> </ul> Source code in <code>src/tests/test_user.py</code> <pre><code>class TestUser(unittest.TestCase):\n    \"\"\"\n    Unit tests for the User class.\n\n    Tests:\n        - Attribute initialization\n        - Password verification\n        - Salted password generation\n    \"\"\"\n\n    def setUp(self):\n        \"\"\"\n        Set up a User instance for testing.\n        \"\"\"\n        self.user = User(\"Alice\", 30, \"secure123\")\n\n    def test_password_verification(self):\n        \"\"\"\n        Test that password verification works correctly.\n        \"\"\"\n        self.assertTrue(self.user.verify_password(\"secure123\"), \"Password should be verified correctly.\")\n        self.assertFalse(self.user.verify_password(\"wrongpass\"), \"Incorrect password should not be verified.\")\n\n    def test_salted_password(self):\n        \"\"\"\n        Test that salted password is a string and differs from the original hash.\n        \"\"\"\n        salted = self.user.salt_password(\"mysalt\")\n        self.assertIsInstance(salted, str, \"Salted password should be a string.\")\n        self.assertNotEqual(salted, self.user._password_hash, \"Salted password should differ from original hash.\")\n\n    def test_attributes(self):\n        \"\"\"\n        Test that user attributes are correctly assigned.\n        \"\"\"\n        self.assertEqual(self.user.name, \"Alice\", \"Name should be 'Alice'.\")\n        self.assertEqual(self.user.age, 30, \"Age should be 30.\")\n</code></pre>"},{"location":"autoapi/tests/test_user/#tests.test_user.TestUser.setUp","title":"<code>setUp()</code>","text":"<p>Set up a User instance for testing.</p> Source code in <code>src/tests/test_user.py</code> <pre><code>def setUp(self):\n    \"\"\"\n    Set up a User instance for testing.\n    \"\"\"\n    self.user = User(\"Alice\", 30, \"secure123\")\n</code></pre>"},{"location":"autoapi/tests/test_user/#tests.test_user.TestUser.test_attributes","title":"<code>test_attributes()</code>","text":"<p>Test that user attributes are correctly assigned.</p> Source code in <code>src/tests/test_user.py</code> <pre><code>def test_attributes(self):\n    \"\"\"\n    Test that user attributes are correctly assigned.\n    \"\"\"\n    self.assertEqual(self.user.name, \"Alice\", \"Name should be 'Alice'.\")\n    self.assertEqual(self.user.age, 30, \"Age should be 30.\")\n</code></pre>"},{"location":"autoapi/tests/test_user/#tests.test_user.TestUser.test_password_verification","title":"<code>test_password_verification()</code>","text":"<p>Test that password verification works correctly.</p> Source code in <code>src/tests/test_user.py</code> <pre><code>def test_password_verification(self):\n    \"\"\"\n    Test that password verification works correctly.\n    \"\"\"\n    self.assertTrue(self.user.verify_password(\"secure123\"), \"Password should be verified correctly.\")\n    self.assertFalse(self.user.verify_password(\"wrongpass\"), \"Incorrect password should not be verified.\")\n</code></pre>"},{"location":"autoapi/tests/test_user/#tests.test_user.TestUser.test_salted_password","title":"<code>test_salted_password()</code>","text":"<p>Test that salted password is a string and differs from the original hash.</p> Source code in <code>src/tests/test_user.py</code> <pre><code>def test_salted_password(self):\n    \"\"\"\n    Test that salted password is a string and differs from the original hash.\n    \"\"\"\n    salted = self.user.salt_password(\"mysalt\")\n    self.assertIsInstance(salted, str, \"Salted password should be a string.\")\n    self.assertNotEqual(salted, self.user._password_hash, \"Salted password should differ from original hash.\")\n</code></pre>"}]}